# Auto-Brightness

Automatically sets screen brightness at boot based on webcam-captured ambient light.

## Features
- Uses webcam to capture light level
- Adjusts brightness using `brightnessctl`
- Configured to run once at boot using `systemd`

## Usage

```bash
python3 scripts/auto_brightness.py
